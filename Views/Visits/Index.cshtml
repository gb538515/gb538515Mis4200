@model IEnumerable<gb538515Mis4200.Models.Visit>

@{
    ViewBag.Title = "Visit Information";
}

<h2>Visit Information</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.visitCost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pawExamination)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.furHealth)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.heartRate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.visitCost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pawExamination)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.furHealth)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.heartRate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.visitID }) |
            @Html.ActionLink("Details", "Details", new { id=item.visitID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.visitID })
        </td>
    </tr>
}

</table>
